<Page x:Class="EasyWin.Views.DashboardPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
      mc:Ignorable="d"
      d:DesignHeight="760"
      d:DesignWidth="1000">

        <StackPanel Margin="24,8,24,24" MaxWidth="1020" HorizontalAlignment="Left">

            <DockPanel>
                <StackPanel DockPanel.Dock="Right" VerticalAlignment="Center" TextBlock.TextAlignment="Right">
                    <TextBlock Text="{Binding ComputerName}" FontSize="13" FontWeight="SemiBold" HorizontalAlignment="Right"
                               Foreground="{DynamicResource TextFillColorPrimaryBrush}" />
                    <TextBlock Text="{Binding UserName, StringFormat=用户:{0}}" FontSize="11"
                               Foreground="{DynamicResource TextFillColorSecondaryBrush}" HorizontalAlignment="Right" />
                </StackPanel>
                <StackPanel>
                    <TextBlock Text="仪表盘" Style="{StaticResource PageTitleText}" />
                    <TextBlock Text="系统与网络概览" Style="{StaticResource CaptionText}" Margin="0,4,0,0" />
                </StackPanel>
            </DockPanel>

            <!-- 概览卡片:第一行 -->
            <Grid Margin="0,18,0,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <ui:Card Grid.Column="0" Margin="0,0,12,12" Padding="16">
                    <StackPanel>
                        <StackPanel Orientation="Horizontal">
                            <ui:SymbolIcon Symbol="Desktop24" FontSize="18" Foreground="{DynamicResource TextFillColorSecondaryBrush}" />
                            <TextBlock Text="操作系统" Margin="8,0,0,0" Style="{StaticResource CaptionText}" VerticalAlignment="Center" />
                        </StackPanel>
                        <TextBlock Text="{Binding OsName}" FontSize="15" FontWeight="SemiBold" Margin="0,10,0,0"
                                   TextTrimming="CharacterEllipsis" ToolTip="{Binding OsName}" />
                        <TextBlock Margin="0,4,0,0" Style="{StaticResource CaptionText}">
                            <Run Text="{Binding OsVersion, Mode=OneWay}" />
                            <Run Text="{Binding OsArch, Mode=OneWay}" />
                        </TextBlock>
                    </StackPanel>
                </ui:Card>

                <ui:Card Grid.Column="1" Margin="0,0,12,12" Padding="16">
                    <StackPanel>
                        <StackPanel Orientation="Horizontal">
                            <ui:SymbolIcon Symbol="ShieldCheckmark24" FontSize="18" Foreground="{DynamicResource TextFillColorSecondaryBrush}" />
                            <TextBlock Text="激活状态" Margin="8,0,0,0" Style="{StaticResource CaptionText}" VerticalAlignment="Center" />
                        </StackPanel>
                        <TextBlock Text="{Binding ActivationText}" FontSize="15" FontWeight="SemiBold" Margin="0,10,0,0" />
                        <TextBlock Text="软件许可服务平台" Style="{StaticResource CaptionText}" Margin="0,4,0,0" />
                    </StackPanel>
                </ui:Card>

                <ui:Card Grid.Column="2" Margin="0,0,0,12" Padding="16">
                    <StackPanel>
                        <StackPanel Orientation="Horizontal">
                            <ui:SymbolIcon Symbol="Router24" FontSize="18" Foreground="{DynamicResource TextFillColorSecondaryBrush}" />
                            <TextBlock Text="网络" Margin="8,0,0,0" Style="{StaticResource CaptionText}" VerticalAlignment="Center" />
                        </StackPanel>
                        <TextBlock Text="{Binding ActiveNetwork}" FontSize="15" FontWeight="SemiBold" Margin="0,10,0,0"
                                   TextTrimming="CharacterEllipsis" ToolTip="{Binding ActiveNetwork}" />
                        <StackPanel Orientation="Horizontal" Margin="0,4,0,0">
                            <Ellipse Width="8" Height="8" VerticalAlignment="Center" Margin="0,0,6,0">
                                <Ellipse.Style>
                                    <Style TargetType="Ellipse">
                                        <Setter Property="Fill" Value="#8A8A8A" />
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding InternetOk}" Value="True">
                                                <Setter Property="Fill" Value="#5CB87A" />
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </Ellipse.Style>
                            </Ellipse>
                            <TextBlock Text="{Binding InternetText}" Style="{StaticResource CaptionText}" />
                            <TextBlock Text="{Binding AdapterCountText}" Style="{StaticResource CaptionText}" Margin="12,0,0,0" />
                        </StackPanel>
                    </StackPanel>
                </ui:Card>
            </Grid>

            <!-- 概览卡片:第二行 -->
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <ui:Card Grid.Column="0" Margin="0,0,12,12" Padding="16">
                    <StackPanel>
                        <StackPanel Orientation="Horizontal">
                            <ui:SymbolIcon Symbol="Settings24" FontSize="18" Foreground="{DynamicResource TextFillColorSecondaryBrush}" />
                            <TextBlock Text="处理器" Margin="8,0,0,0" Style="{StaticResource CaptionText}" VerticalAlignment="Center" />
                        </StackPanel>
                        <Grid Margin="0,10,0,0">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="{Binding CpuName}" FontSize="13" TextTrimming="CharacterEllipsis"
                                       ToolTip="{Binding CpuName}" VerticalAlignment="Center" />
                            <TextBlock Grid.Column="1" Text="{Binding CpuPercent, StringFormat={}{0:0}%}" FontSize="15"
                                       FontWeight="SemiBold" Margin="10,0,0,0" VerticalAlignment="Center" />
                        </Grid>
                        <ProgressBar Value="{Binding CpuPercent, Mode=OneWay}" Maximum="100" Height="5" Margin="0,10,0,0"
                                     Foreground="{DynamicResource AccentFillColorDefaultBrush}" />
                    </StackPanel>
                </ui:Card>

                <ui:Card Grid.Column="1" Margin="0,0,12,12" Padding="16">
                    <StackPanel>
                        <StackPanel Orientation="Horizontal">
                            <ui:SymbolIcon Symbol="Grid24" FontSize="18" Foreground="{DynamicResource TextFillColorSecondaryBrush}" />
                            <TextBlock Text="内存" Margin="8,0,0,0" Style="{StaticResource CaptionText}" VerticalAlignment="Center" />
                        </StackPanel>
                        <Grid Margin="0,10,0,0">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>
                            <TextBlock Style="{StaticResource CaptionText}" VerticalAlignment="Center">
                                <Run Text="{Binding MemUsedText, Mode=OneWay}" FontSize="15" FontWeight="SemiBold"
                                     Foreground="{DynamicResource TextFillColorPrimaryBrush}" />
                                <Run Text="/" />
                                <Run Text="{Binding MemTotalText, Mode=OneWay}" />
                            </TextBlock>
                            <TextBlock Grid.Column="1" Text="{Binding MemPercent, StringFormat={}{0:0}%}" FontSize="15"
                                       FontWeight="SemiBold" Margin="10,0,0,0" VerticalAlignment="Center" />
                        </Grid>
                        <ProgressBar Value="{Binding MemPercent, Mode=OneWay}" Maximum="100" Height="5" Margin="0,10,0,0"
                                     Foreground="{DynamicResource AccentFillColorDefaultBrush}" />
                    </StackPanel>
                </ui:Card>

                <ui:Card Grid.Column="2" Margin="0,0,0,12" Padding="16">
                    <StackPanel>
                        <StackPanel Orientation="Horizontal">
                            <ui:SymbolIcon Symbol="Clock24" FontSize="18" Foreground="{DynamicResource TextFillColorSecondaryBrush}" />
                            <TextBlock Text="运行时长" Margin="8,0,0,0" Style="{StaticResource CaptionText}" VerticalAlignment="Center" />
                        </StackPanel>
                        <TextBlock Text="{Binding UptimeText}" FontSize="15" FontWeight="SemiBold" Margin="0,10,0,0" />
                        <TextBlock Text="自上次开机起" Style="{StaticResource CaptionText}" Margin="0,4,0,0" />
                    </StackPanel>
                </ui:Card>
            </Grid>

            <!-- 磁盘 -->
            <TextBlock Text="磁盘" Style="{StaticResource SectionTitleText}" />
            <ItemsControl ItemsSource="{Binding Disks}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <WrapPanel />
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <ui:Card Width="326" Margin="0,0,12,12" Padding="16" Cursor="Hand" ToolTip="双击打开磁盘">
                            <ui:Card.InputBindings>
                                <MouseBinding Gesture="LeftDoubleClick"
                                              Command="{Binding DataContext.OpenDiskCommand, RelativeSource={RelativeSource AncestorType=Page}}"
                                              CommandParameter="{Binding}" />
                            </ui:Card.InputBindings>
                            <StackPanel>
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto" />
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="Auto" />
                                    </Grid.ColumnDefinitions>
                                    <ui:SymbolIcon Symbol="Storage24" FontSize="17" Foreground="{DynamicResource TextFillColorSecondaryBrush}" />
                                    <TextBlock Grid.Column="1" Margin="8,0,0,0" FontSize="13" FontWeight="SemiBold" VerticalAlignment="Center">
                                        <Run Text="{Binding Drive, Mode=OneWay}" />
                                        <Run Text="{Binding Label, Mode=OneWay}" Foreground="{DynamicResource TextFillColorTertiaryBrush}" FontSize="11" />
                                    </TextBlock>
                                    <TextBlock Grid.Column="2" Text="{Binding UsageText}" Style="{StaticResource CaptionText}" VerticalAlignment="Center" />
                                </Grid>
                                <ProgressBar Value="{Binding UsagePercent, Mode=OneWay}" Maximum="100" Height="5" Margin="0,10,0,0"
                                             Foreground="{DynamicResource AccentFillColorDefaultBrush}" />
                            </StackPanel>
                        </ui:Card>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>

        </StackPanel>
</Page>
