<ui:FluentWindow x:Class="EasyWin.Views.AutomationEditWindow"
                 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                 xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                 xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                 xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
                 mc:Ignorable="d"
                 Title="自动化规则"
                 Width="480"
                 SizeToContent="Height"
                 MaxHeight="600"
                 WindowStartupLocation="CenterOwner"
                 ResizeMode="NoResize"
                 ExtendsContentIntoTitleBar="True"
                 WindowBackdropType="Mica"
                 WindowCornerPreference="Round"
                 FontFamily="{DynamicResource AppFont}">

    <Grid Margin="20,4,20,20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <ui:TitleBar Grid.Row="0" Title="{Binding Title, RelativeSource={RelativeSource AncestorType=ui:FluentWindow}}" Padding="0,6" Background="Transparent" />

        <StackPanel Grid.Row="1" Margin="0,8,0,0">
            <TextBlock Text="触发条件:连接到 Wi-Fi" Style="{StaticResource FieldLabelText}" />
            <ComboBox x:Name="SsidBox" IsEditable="True" />

            <TextBlock Text="执行动作:应用 IP 方案" Style="{StaticResource FieldLabelText}" Margin="0,14,0,0" />
            <ComboBox x:Name="ProfileBox" />

            <StackPanel Orientation="Horizontal" Margin="0,14,0,0">
                <CheckBox x:Name="EnabledBox" IsChecked="True" Content="启用规则" VerticalAlignment="Center" />
                <TextBlock Text="命中后自动切换网卡配置,托盘需保持常驻" Style="{StaticResource CaptionText}" Margin="12,0,0,0" VerticalAlignment="Center" />
            </StackPanel>

            <Grid Margin="0,22,0,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <ui:Button Grid.Column="0" Content="取消" Appearance="Secondary" Padding="20,7"
                           IsCancel="True" />
                <ui:Button Grid.Column="1" Content="保存规则" Icon="{ui:SymbolIcon Save24}" Appearance="Primary" Padding="20,7"
                           IsDefault="True" Click="OnSave" />
            </Grid>
        </StackPanel>
    </Grid>
</ui:FluentWindow>
