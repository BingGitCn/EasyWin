<ui:FluentWindow x:Class="EasyWin.Views.WlanPasswordWindow"
                 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                 xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                 xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                 xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
                 mc:Ignorable="d"
                 Title="连接 Wi-Fi"
                 Width="440"
                 SizeToContent="Height"
                 WindowStartupLocation="CenterOwner"
                 ResizeMode="NoResize"
                 ExtendsContentIntoTitleBar="True"
                 WindowBackdropType="Mica"
                 WindowCornerPreference="Round"
                 FontFamily="{DynamicResource AppFont}">

    <Grid Margin="20,4,20,20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <ui:TitleBar Grid.Row="0" Title="{Binding Title, RelativeSource={RelativeSource AncestorType=ui:FluentWindow}}" Padding="0,6" Background="Transparent" />

        <StackPanel Grid.Row="1" Margin="0,8,0,0">
            <TextBlock Text="{Binding Ssid, RelativeSource={RelativeSource AncestorType=ui:FluentWindow}}"
                       FontSize="16" FontWeight="SemiBold" />

            <TextBlock x:Name="AuthText" Text="" Style="{StaticResource CaptionText}" Margin="0,4,0,0" />

            <TextBlock Text="网络安全密钥(密码)" Style="{StaticResource FieldLabelText}" Margin="0,16,0,0" />
            <PasswordBox x:Name="PasswordBox" />

            <TextBlock Text="提示:若该网络之前保存过密码,可留空直接用已保存的配置连接。"
                       Style="{StaticResource CaptionText}" Margin="0,10,0,0" TextWrapping="Wrap" />

            <Grid Margin="0,20,0,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <ui:Button Grid.Column="0" Content="取消" Appearance="Secondary" Padding="20,7" IsCancel="True" />
                <ui:Button Grid.Column="1" Content="连接" Icon="{ui:SymbolIcon Wifi124}" Appearance="Primary" Padding="20,7"
                           IsDefault="True" Click="OnConnect" />
            </Grid>
        </StackPanel>
    </Grid>
</ui:FluentWindow>
